### FUNCTIONS


def factorial(current_number, accumulated_sum):
    #if current_number == 6:
     #   return accumulated_sum
    #else:
      #  return(factorial)(
       #     current_number + 1,
        #    accumulated_sum * current_number
        #)
    
    while current_number < 6:
        accumulated_sum * current_number
        current_number + 1
    else:
        return accumulated_sum
    

print(factorial(current_number=1, accumulated_sum=1))